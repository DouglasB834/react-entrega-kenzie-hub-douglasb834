import styled from "styled-components";

export const MainStyled = styled.main`
    gap:1rem;
    width: 98%;
    display:flex;
    flex-direction: column;


    .container{
        margin:  0 auto;
        max-width: 780px;
        color: var(--grey-0);
    }


`;
